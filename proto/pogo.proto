syntax="proto3";

package uk.org.empty.pogo;

service Pogo {
    rpc Add (Task) returns (Unit) { }
    rpc List (Unit) returns (stream Task) { }
}

message Unit {
}

message Task {
    string title = 1;
}
